//
// Generated by SwiftPoet
// https://github.com/outfoxx/swiftpoet
//
import Foundation

public extension API.Communication {
    /**
     * Asks the client for permission to redirect to another web app than the current one.
     * The client can decide whether the app switch should be allowed or disallowed and intercepted by the client
     * (e.g. a client with an own map might not want the web app to redirect to the fuel-station-finder).
     * By default, redirects should always be allowed.
     */
    struct AppRedirectRequest: Codable {
        /**
         * The app the current PWA is redirecting to, e.g. `fuel-station-finder`, `fueling`, `pay`, or `legal` */
        public let app: String
    }
}
